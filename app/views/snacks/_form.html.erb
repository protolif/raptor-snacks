<%= form_with(model: snack, local: true) do |form| %>
  <% if snack.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(snack.errors.count, "error") %> prohibited this snack from being saved:</h2>

      <ul>
      <% snack.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :pdf_version %>
    <%= form.text_field :pdf_version %>
  </div>

  <div class="field">
    <%= form.label :page_count %>
    <%= form.number_field :page_count %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
